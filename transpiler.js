const Commands = require("./commands");


module.exports = {

    Transpile: function (code) {
        let cmds = [];

        // am bad

        return Commands.GenerateMultipleCommands(cmds);
    }
};